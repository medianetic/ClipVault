# Project Instructions

## Workflows

### Versioning
- **Feature Additions:** Every time a new feature is added, the app version in `package.json` must be incremented.
- **Convention:** Use Semantic Versioning (SemVer). For new features, increment the minor or patch version as appropriate.
